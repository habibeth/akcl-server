import { TViewer } from "./viewer.interface";




const createViewerIntoDB = async (payload: TViewer) => {
    // const result = await Viewer.create(payload);
    return null;
}

export const ViewerServices = {
    createViewerIntoDB
}